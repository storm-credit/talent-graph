# TalentGraph

Ontology-driven workforce simulation engine for role fit, department placement, and performance prediction.

## Quickstart

```bash
pip install -e ".[dev]"
talentgraph evaluate --person "Alice Chen" --top 3 --explain
```
